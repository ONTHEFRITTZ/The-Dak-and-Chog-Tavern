// Forwarder for MetaMask Delegation Toolkit v0.13.0.
// Keep fully-qualified URLs so import() from a Blob succeeds without base issues.
export * from 'https://esm.sh/@metamask/delegation-toolkit@0.13.0?bundle&target=es2022';

