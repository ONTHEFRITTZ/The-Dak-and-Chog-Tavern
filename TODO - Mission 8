Mission 8 - Poker AA Build

Completed
- Gasless "Smart Account" UX via 4337: AA client bootstrap in `js/aaClient.js` using Monad bundler + paymaster; falls back to signer safely.
- AA UI simplified: `js/aa/ui.js` shows Wallet, Gasless Mode, Network, Gas; single renderButtons (no delegation UI). 7702 readiness indicator added.
- 7702 detector: `js/aa/init-all.js` imports vendor via Blob to avoid MIME; emits `aa:7702` + localStorage flag; defaults to Gasless Mode.
- AA ops: `js/aa/ops.js` cleaned; robust `sendTxViaAA` (client/smartAccount/window fallback).
- Bundler detection: `js/bundler.js` only uses `wallet_sendCalls` when capability exists; safe fallback.
- Poker flow: AA ops imported; joinSeat/leaveSeat route through AA then signer fallback (no -32603 RPC errors).
- Envio activity: `js/envio-activity.js` renders recent events; `getActiveScoreFor(address)` supports LST rewards.

Ready To Verify / Playtest
- Landing: Enable Gasless Mode → Poker → Sit. Join succeeds via bundler or signer fallback (no -32603).
- Agent indicators render; verify gasless sends under budget.
- Envio panel shows activity when ENVIO_HYPERSYNC_URL is set (or localStorage key).
- 7702 indicator shows `not-ready`; detector fires `aa:7702` when ready.

In Progress (today)
- Add small AA UI pills (done): `SA(7702): ready/not-ready`, and "Activity: N" (Envio).
- Wire Envio endpoint on prod and validate activity + score.
- Agent UX: toggles in Poker (Auto Ready/Auto Rebuy/Auto Clear Seat) with localStorage flags; route via AA.
- README/HACKATHON: demo flow + 7702 plan.

Next (post-demo)
- DCmon swaps once DEX liquidity is live.
- Buy-in modal with DCmon + swap UI; tie rewards to `getActiveScoreFor`.
- Envio indexer schema for DCmon buy-ins/rewards/seat events; dashboards.
