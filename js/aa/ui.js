// at the very top of aa/ui.jsconst __MODE = (document.documentElement.getAttribute('data-table-mode') || '').toLowerCase();const __IS_F2P = __MODE === 'f2p';import { ensureMonadSelected, getAccounts } from './account.js';import { MONAD } from './config.js';import { AA, initAA } from '../aaClient.js';function short(addr) { return addr && addr.length > 10 ? `${addr.slice(0, 6)}...${addr.slice(-4)}` : (addr || ''); }const SMART_ACCOUNT_OPT_IN_KEY = 'aa.smartAccount.optIn';function ensureContainer() {  if (__IS_F2P) return document.createElement('div');  let c = document.getElementById('aa-controls');  const preferredHost = document.getElementById('wi-aa-panel-host');  const host = preferredHost || document.getElementById('wallet-inline') || document.body;  if (!c) {    c = document.createElement('div'); c.id = 'aa-controls';    c.style.cssText = ['display:flex','flex-direction:column','gap:6px','align-items:flex-start','margin-left:8px','max-width:360px','font-size:12px','color:#f5f5f5'].join(';');    host.appendChild(c);  } else if (host && c.parentElement !== host) { host.appendChild(c); }  if (preferredHost) { c.style.display='flex'; c.style.marginLeft='0'; c.style.width='100%'; c.style.maxWidth='100%'; }  else { c.style.display='none'; c.style.marginLeft='8px'; c.style.maxWidth='360px'; c.style.width=''; }  return c;}function ensureStatusBox() {  const c = ensureContainer();  let box = c.querySelector('.aa-status');  if (!box) { box = document.createElement('div'); box.className='aa-status'; box.style.cssText=['display:flex','flex-wrap:wrap','gap:6px','align-items:center','background:rgba(0,0,0,0.45)','padding:6px 10px','border-radius:10px','font-weight:600'].join(';'); c.appendChild(box); }  return box;}function ensureActionsBox() { const c = ensureContainer(); let box = c.querySelector('#aa-actions'); if (!box) { box=document.createElement('div'); box.id='aa-actions'; box.style.cssText=['display:flex','flex-wrap:wrap','gap:6px','align-items:center'].join(';'); c.appendChild(box);} return box; }function setBusy(button, busy, labelWhenBusy = 'Working...') { if (!button) return; if (busy) { if (!button.dataset.label) button.dataset.label = button.textContent || button.value || ''; button.disabled = true; button.textContent = labelWhenBusy; } else { const original = button.dataset.label || button.textContent; button.disabled = false; button.textContent = original; } }function renderStatus(state) {  const { addr, controller, smartAddress, smartType, chainOk, sponsored } = state;  const box = ensureStatusBox();  const statusBits = [];  if (!controller) statusBits.push("Wallet: not connected"); else statusBits.push(`Wallet: ${short(controller)}`);  if (smartType === "aa4337") statusBits.push(`Gasless Mode: ${short(smartAddress)}`); else statusBits.push("Gasless Mode: fallback (EOA)");  const netLabel = chainOk ? `Network: ${MONAD.name || "Monad Testnet"}` : "Network: switch to Monad";  statusBits.push(netLabel);  if (sponsored != null) statusBits.push(`Gas: ${sponsored ? "sponsored" : "self-pay"}`);  // SA(7702) readiness badge  let sa7702 = "not-ready";  try { sa7702 = (localStorage.getItem("aa.7702.ready") === "true") ? "ready" : "not-ready"; } catch {}  statusBits.push(`SA(7702): ${sa7702}`);  box.textContent = statusBits.join(" | ");  // Tooltip info  try {    let tip = box.querySelector(".aa-tip-7702");    if (!tip) { tip = document.createElement("span"); tip.className="aa-tip-7702"; tip.style.cssText="margin-left:6px;opacity:.9;cursor:help"; tip.textContent="?"; box.appendChild(tip); }    tip.title = "When SA(7702) is ready, we switch to internal-signer delegation (derive SA ? minimal open delegation ? mmAccount.signDelegation). No extra clicks; no re-sign loops.";  } catch {}}async function hydrate() {  if (__IS_F2P) return;  let chainOk = false; try { chainOk = await ensureMonadSelected({ requestSwitch: false }); } catch {}  const accounts = await getAccounts(); const addr = (accounts[0] || '').toLowerCase();  const smartOptIn = (localStorage.getItem(SMART_ACCOUNT_OPT_IN_KEY) === 'true');  let smartType = 'fallback'; try { if (smartOptIn) { await initAA({}); smartType = AA.smartAccountType || (AA.sponsored ? 'aa4337' : 'fallback'); } } catch {}  const controllerAddr = AA.controllerAddress || addr || null;  const state = { addr, controller: controllerAddr, smartAddress: addr, smartType, chainOk, sponsored: !!AA.sponsored };  renderStatus(state);  await renderButtons(state);}window.addEventListener('load', () => { hydrate().catch(() => {}); });window.addEventListener('wallet:connected', () => { hydrate().catch(() => {}); });window.addEventListener('aa:smartaccount', () => { hydrate().catch(() => {}); });window.addEventListener('aa:session', () => { hydrate().catch(() => {}); });window.addEventListener('aa:7702', () => { hydrate().catch(() => {}); });function renderButtons(state) {  const { controller, chainOk } = state;  const actions = ensureActionsBox(); actions.innerHTML = '';  const makeButton = (label) => { const b=document.createElement('button'); b.className='aa-btn'; b.style.cssText=['padding:6px 10px','border-radius:8px','border:none','background:#2d6aee','color:#fff','font-weight:600','cursor:pointer'].join(';'); b.textContent=label; b.addEventListener('mouseenter',()=>{ if(!b.disabled) b.style.background='#2f7bff';}); b.addEventListener('mouseleave',()=>{ b.style.background='#2d6aee';}); return b; };  const safeRun = (btn, fn, labelBusy) => { btn.addEventListener('click', async ()=>{ if(btn.disabled) return; setBusy(btn, true, labelBusy); try{ await fn(); } catch(e){ console.error('[aa/ui] action failed', e); alert(e?.message||'Operation failed.'); } finally { setBusy(btn, false); hydrate().catch(()=>{}); } }); };  if (!controller) { const note=document.createElement('span'); note.style.cssText='font-size:11px;color:rgba(255,255,255,0.8)'; note.textContent='Connect MetaMask to Enable Gasless Mode controls.'; actions.appendChild(note); return; }  if (!chainOk) { const switchBtn=makeButton('Switch to Monad'); safeRun(switchBtn, async()=>{ const ok = await ensureMonadSelected({ requestSwitch:true }); if(!ok) throw new Error('Wallet did not switch to Monad.'); }, 'Switching...'); actions.appendChild(switchBtn); return; }  const smartOptIn = (window.AA && AA.sponsored) ? true : (localStorage.getItem('aa.smartAccount.optIn')==='true');  if (!smartOptIn) { const enableBtn=makeButton('Enable Gasless Mode'); safeRun(enableBtn, async()=>{ try{ localStorage.setItem('aa.smartAccount.optIn','true'); }catch{} try{ await initAA({}); AA.setSponsored(true); }catch{} }, 'Enabling...'); actions.appendChild(enableBtn); return; }  const disableBtn=makeButton('Disable Gasless Mode'); safeRun(disableBtn, async()=>{ try{ localStorage.setItem('aa.smartAccount.optIn','false'); }catch{} try{ await initAA({}); AA.setSponsored(false); }catch{} }, 'Disabling...'); actions.appendChild(disableBtn);  try { const envioUrl = (typeof window.ENVIO_HYPERSYNC_URL==='string' && window.ENVIO_HYPERSYNC_URL) ? window.ENVIO_HYPERSYNC_URL : (localStorage.getItem('envio.hypersync.url')||''); if (envioUrl) { const spot=document.createElement('span'); spot.style.cssText='font-size:11px;color:rgba(255,255,255,0.9);margin-left:6px;'; spot.textContent='Activity: …'; actions.appendChild(spot); import('/js/envio-activity.js').then(m=>m.getActiveScoreFor(state.controller)).then(s=>{ spot.textContent='Activity: '+s; }).catch(()=>{ spot.textContent='Activity: n/a'; }); } } catch {}}