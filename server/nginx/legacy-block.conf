## Repo-managed include for Nginx (intentionally empty for 1.1 Stable rollback)
# This file intentionally contains no location blocks to avoid any
# duplicate-location conflicts while restoring v1.1 behavior.
#
# When ready to isolate Poker again, we will reintroduce only the
# required blocks here and ensure the include appears exactly once
# in the server block.
